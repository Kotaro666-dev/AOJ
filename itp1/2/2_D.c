/*
長方形の中に円が含まれるかを判定するプログラムを作成してください。
次のように、長方形は左下の頂点を原点とし、右上の頂点の座標 (W,H) が与えられます。
また、円はその中心の座標 (x,y) と半径 r で与えられます。
 */

#include <stdio.h>

int main(void)
{
    int width, height, x, y, radius;

    scanf("%d %d %d %d %d", &width, &height, &x, &y, &radius);

    // exception
    if ((r > 0 && r < 3) && (W < 2 && )


    // x pattern
    if ()

    // y pattern
    return (0);
}